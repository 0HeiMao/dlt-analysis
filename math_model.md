# 数学模型与公式说明

> 配套工具：`dlt_tool.py` v1.0.0
> 前置声明：超级大乐透每期开奖为**相互独立的随机事件**。本文档所有公式只服务于两个目的——描述历史样本、检验历史样本是否偏离均匀随机模型。**任何公式都无法预测未来开奖结果。**

---

## 0. 记号与基本参数

| 符号 | 含义 | 取值 |
|---|---|---|
| N_f | 前区号码池大小 | 35（号码 01–35） |
| k_f | 前区抽取个数 | 5（互不重复） |
| N_b | 后区号码池大小 | 12（号码 01–12） |
| k_b | 后区抽取个数 | 2（互不重复） |
| C(n, k) | 组合数 n!/((n−k)!·k!)，代码中用 `math.comb(n, k)` 精确计算 | — |
| n | 样本期数（描述统计中） | 演示集为 600 |
| α | 显著性水平 | 0.05 |

前区与后区**相互独立**抽取，先算前区再算后区，二者概率直接相乘。

---

## 1. 组合总数（样本空间大小）

前区组合数：

```
C(35,5) = 35! / (5! · 30!)
        = (35×34×33×32×31) / (5×4×3×2×1)
        = 38,955,840 / 120
        = 324,632
```

后区组合数：

```
C(12,2) = 12! / (2! · 10!) = (12×11)/2 = 66
```

单注总组合数（样本空间）：

```
Ω = C(35,5) × C(12,2) = 324,632 × 66 = 21,425,712
```

即任意一注 5+2 组合命中一等奖的概率为 **1/21,425,712 ≈ 4.667×10⁻⁸**。

代码对照：`count_front_combinations()`、`count_back_combinations()`、`count_total_combinations()`。

---

## 2. 超几何分布与奖级中奖概率

### 2.1 单情形命中概率

开奖是"从 35 个号中无放回抽 5 个"，你的彩票固定含 5 个前区号。命中 a 个前区号属于超几何分布：

```
P_f(a) = C(5, a) · C(30, 5−a) / C(35,5)          （a = 0..5）
```

分子含义：从你选的 5 个号中命中 a 个（C(5,a) 种），再从其余 30 个号中"错选" 5−a 个（C(30,5−a) 种）。

同理，后区命中 b 个：

```
P_b(b) = C(2, b) · C(10, 2−b) / C(12,2)          （b = 0..2）
```

前后区独立，故情形 (a, b) 的概率与组合数：

```
P(a,b) = P_f(a) × P_b(b)
N(a,b) = C(5,a)·C(30,5−a) × C(2,b)·C(10,2−b)
```

所有 36 种 (a,b) 情形两两**互斥**且穷尽（Σ N(a,b) = 21,425,712，工具内置断言校验）。

### 2.2 九个奖级的概率表

奖级定义（前区命中 a + 后区命中 b；八、九等奖含多个子情形，子情形间互斥、直接相加）：

| 奖级 | 命中要求 (a,b) | 组合数 N | 概率公式 | 概率数值 | 约等于 |
|---|---|---|---|---|---|
| 一等奖 | (5,2) | 1 | 1/Ω | 4.66729×10⁻⁸ | 1/21,425,712 |
| 二等奖 | (5,1) | 20 | C(5,5)C(30,0)·C(2,1)C(10,1) / Ω | 9.33458×10⁻⁷ | 1/1,071,286 |
| 三等奖 | (5,0) | 45 | 1 × C(2,0)C(10,2) / Ω | 2.10028×10⁻⁶ | 1/476,127 |
| 四等奖 | (4,2) | 150 | C(5,4)C(30,1) × 1 / Ω | 7.00093×10⁻⁶ | 1/142,838 |
| 五等奖 | (4,1) | 3,000 | 150 × 20 / Ω | 1.40019×10⁻⁴ | 1/7,141.9 |
| 六等奖 | (3,2) | 4,350 | C(5,3)C(30,2) × 1 / Ω | 2.03027×10⁻⁴ | 1/4,925.5 |
| 七等奖 | (4,0) | 6,750 | 150 × 45 / Ω | 3.15042×10⁻⁴ | 1/3,174.2 |
| 八等奖 | (3,1) + (2,2) | 87,000 + 40,600 = 127,600 | [C(5,3)C(30,2)·C(2,1)C(10,1) + C(5,2)C(30,3)·1] / Ω | 5.95546×10⁻³ | 1/167.9 |
| 九等奖 | (3,0)+(1,2)+(2,1)+(0,2) | 195,750+137,025+812,000+142,506 = 1,287,281 | 四个子情形相加 / Ω | 6.00811×10⁻² | 1/16.64 |

九等奖子情形明细：
- (3,0)：C(5,3)C(30,2) × C(2,0)C(10,2) = 4,350 × 45 = 195,750
- (1,2)：C(5,1)C(30,4) × 1 = 5 × 27,405 = 137,025
- (2,1)：C(5,2)C(30,3) × C(2,1)C(10,1) = 40,600 × 20 = 812,000
- (0,2)：C(5,0)C(30,5) × 1 = 142,506

**至少中任一奖级**（各奖级互斥，直接求和）：

```
P(中奖) = 1,429,197 / 21,425,712 = 0.0667047611 ≈ 1/14.99
P(未中奖) = 19,996,515 / 21,425,712 = 0.9332952389
P(中奖) + P(未中奖) = 1  （自洽）
```

代码对照：`hypergeometric_probability()`、`case_probability()`、`case_count()`、`prize_probabilities()`、`any_prize_probability()`、`self_consistency_check()`（自洽断言）与 `monte_carlo_probability_check()`（蒙特卡洛交叉校验）。

### 2.3 自洽性断言（工具内置，必须通过）

1. Σ 全部 36 种 (a,b) 情形组合数 = 21,425,712（穷举互斥完备）；
2. 中奖组合数 + 未中奖组合数 = 总组合数；
3. 计数法概率（组合数/Ω）与公式法概率（超几何连乘）之差 = 0；
4. 九等奖子情形计数之和 = 奖级表合计。

`python dlt_tool.py selftest` 逐条运行上述断言并打印 PASSED/FAILED，另附 200,000 次蒙特卡洛抽样交叉校验。

---

## 3. 数学期望值与返奖率

设奖级 t 的单注奖金为 m_t（单位：元），单注投注成本为 c = 2 元（基本投注）：

```
每注期望毛回报   E = Σ_t P(t) · m_t
每注净期望       E_net = E − c
每 1 元期望回报  r = E / c
返奖率(期望)     = r （长期每投入 1 元平均收回的金额）
每注期望亏损     = c − E （当 E < c 时）
```

**示例奖金字典（非官方承诺，可用 `--prize-amounts` 自定义）**：一等 8,000,000 / 二等 150,000 / 三等 10,000 / 四等 3,000 / 五等 300 / 六等 200 / 七等 100 / 八等 15 / 九等 5。

代入计算（见 `example_output.md` 真实输出）：

```
E = 1.059260 元, E_net = −0.940740 元, r = 0.529630（返奖率 52.96%）
```

解读：长期平均每注亏 0.94 元；每投入 1 元期望收回约 0.53 元。中国官方规定乐透数字型彩票返奖率约 **51%**，示例字典算出的 52.96% 与之接近（一等奖按固定示例值计，实际一等/二等为浮动奖金，会随奖池波动）。**期望为负是结构性事实**：返奖率 < 100% 决定了任何投注策略（包括倍投、追号）长期必然亏损，倍投只会放大单期风险。

代码对照：`expected_value()`。

---

## 4. 描述性统计：口径与理论值

### 4.1 号码频次与频率

- 口径：`频率 = 该号码历史出现次数 / n`；前区 35 个频率之和恒等于 5，后区 12 个频率之和恒等于 2（每期固定抽 5 个 / 2 个）。
- 理论期望频次：前区单号每期被抽中概率 p_f = 5/35 = 1/7 ≈ 0.142857，期望频次 = n/7；后区 p_b = 2/12 = 1/6 ≈ 0.166667，期望频次 = n/6。演示集 n=600 → 前区期望 85.71 次，后区期望 100 次。

### 4.2 冷热号

- 口径：按出现次数排序的 Top-N / Bottom-N。**"热/冷"只是频率排序的中性描述**，不是号码的属性；每期开奖独立，热号下期被抽中的概率仍是 5/35（前区）或 2/12（后区）。

### 4.3 遗漏值

- 当前遗漏 = 距该号码最近一次出现过了多少期（最后一期出现则为 0）。
- 历史最大遗漏 = 样本期内该号码两次出现之间的最大间隔，**含首现前与末现后的间隔**（口径声明：若号码在样本开始若干期后才首次出现，这段空白计入最大遗漏）。
- 理论视角：每期独立、单号命中概率 p，则"遗漏长度"服从几何分布 Geom(p)（支撑从 0 开始），期望遗漏 ≈ (1−p)/p。前区 p=1/7 → 期望遗漏 ≈ 6 期；演示集平均遗漏 4.5~7.1 期，量级吻合。

### 4.4 和值分布（前区 5 号之和）

理论均值（1..35 均匀无放回抽 5 个之和）：

```
E[S] = k_f × (N_f+1)/2 = 5 × 18 = 90
```

理论方差（无放回有限总体修正）：

```
Var(S) = k_f · σ² · (N_f − k_f)/(N_f − 1)
其中 σ² = (N_f² − 1)/12 = (1225 − 1)/12 = 102   （1..35 的总体方差）
Var(S) = 5 × 102 × 30/34 = 450    →  理论标准差 = √450 ≈ 21.2132
```

演示集观测：均值 89.08 / 标准差 21.44，与理论值（90 / 21.21）吻合。中心极限视角下 S 近似正态，均值±1σ ≈ [68, 111] 覆盖约 68% 期数——这也是 `generate` 中"和值高频区间"约束的来源。

### 4.5 奇偶比（前区奇数个数分布）

前区 1..35 中奇数 18 个、偶数 17 个。奇数个数 K 服从超几何分布：

```
P(K=k) = C(18, k) · C(17, 5−k) / C(35,5),  k = 0..5
```

| k(奇数个数) | 奇:偶 | 理论概率 |
|---|---|---|
| 0 | 0:5 | 0.0191 |
| 1 | 1:4 | 0.1320 |
| 2 | 2:3 | 0.3205 |
| 3 | 3:2 | 0.3419 |
| 4 | 4:1 | 0.1602 |
| 5 | 5:0 | 0.0264 |

### 4.6 大小比（以 18 为界）

小号 01–17 共 17 个，大号 18–35 共 18 个。大号个数 J 同为超几何分布：

```
P(J=j) = C(18, j) · C(17, 5−j) / C(35,5),  j = 0..5
```

与奇偶比同型（都是 18/17 划分），理论概率表与 4.5 完全一致。

### 4.7 区间分布

前区均分为 5 个区间（01–07 / 08–14 / 15–21 / 22–28 / 29–35，各 7 个号）。每期落入某区间的号码个数服从超几何分布，期望 = 5 × 7/35 = **每区每期 1 个**。

### 4.8 连号统计

- 口径：将每期 5 个号升序后，"相邻两号之差为 1"的极大连号组数量（如 {6,7,8,20,21} 含 1 个 3 连组与 1 个 2 连组 → 记 2 组）。
- 理论概率由**精确枚举** C(35,5)=324,632 种前区组合计数得出（0 组 ≈ 0.5234，1 组 ≈ 0.4322，2 组 ≈ 0.0444），非近似公式。

### 4.9 重号统计（相邻两期前区交集）

给定上期的 5 个号，下期与它的交集大小 R 服从超几何分布：

```
P(R=j) = C(5, j) · C(30, 5−j) / C(35,5)
E[R] = k_f² / N_f = 25/35 ≈ 0.7143
```

即平均每期约 0.71 个号码与上一期重复——这是随机抽样的必然结果，**不是"号码记忆"**。

代码对照：`compute_frequency()`、`compute_hot_cold()`、`compute_missing_values()`、`compute_sum_distribution()`、`compute_odd_even_distribution()`、`compute_big_small_distribution()`、`compute_zone_distribution()`、`compute_consecutive_distribution()`、`compute_repeat_distribution()`；理论值函数 `theoretical_sum_mean_variance_formula()`、`theoretical_odd_count_distribution()` 等。

---

## 5. 随机性检验

### 5.1 卡方拟合优度检验

原假设 H0：各号码每期被抽中的概率相等（均匀）。

```
χ² = Σ_i (O_i − E_i)² / E_i
前区: E_i = n·5/35,  i = 1..35,  自由度 df = 35−1 = 34
后区: E_i = n·2/12,  i = 1..12,  自由度 df = 12−1 = 11
```

渐近分布为 χ²(df)。**注意近似性**：同一期内 5 个号互不重复，使 35 个计数之间存在负相关，χ²(k−1) 只是近似参照（其均值偏大）；精确参照以 5.3 的蒙特卡洛经验分布为准。这一说明已写入工具输出。

### 5.2 单号码二项检验 + 多重比较校正

对每个号码单独做二项检验：H0：该号每期命中概率 = 5/35，观测次数 ~ Bin(n, 5/35)。

同时检验 35（前区）/ 12（后区）个号码，未校正的多次检验会使第一类错误膨胀（35 次检验在 α=0.05 下即使全均匀也平均出现约 1.75 个"显著"）。因此必须校正：

- **Bonferroni**：阈值收紧为 α/k（前区 0.05/35 ≈ 0.00143）。保守，控制族错误率 FWER。
- **Benjamini–Hochberg FDR**：控制错误发现率，功效更高。

演示集结果：前区最小原始 p = 0.0085（号码 03），BH 校正后 p = 0.299，**校正后无任何号码显著偏离**。

### 5.3 蒙特卡洛经验 p 值（Phipson–Smyth 校正）

用固定种子模拟 m 块、每块与历史样本同期数的独立均匀开奖，对每块计算同一统计量 T：

```
p_emp = (1 + #{T_sim ≥ T_hist}) / (m + 1)
```

加 1 保证 p_emp > 0 且避免 p=0 的过度自信。经验 p 值不依赖渐近分布假设，天然处理了 5.1 的负相关问题。演示集：m = 100,000 期 ÷ 600 期/块 = 166 块；历史前区 χ²=22.34 的经验 p = 0.844，后区 χ²=3.96 的经验 p = 0.928，均远大于 0.05。

同时模拟**最大遗漏**的经验分布（演示集：均值 55.5、p95 = 70.75），与历史最大遗漏（49）对照——历史遗漏完全落在随机波动范围内。

### 5.4 结论解读纪律（必须随输出附带）

1. p ≥ α → "**不拒绝**均匀随机假设"，即当前样本下未检出显著偏离；
2. **不拒绝 ≠ 证明均匀**：只能说明"没有检出"，且结论受样本量限制（检验功效有限）；
3. 无论检验结果如何，每期开奖独立，历史数据**不能预测未来**。

代码对照：`chi_square_uniform_test()`、`per_number_binomial_test()`、`monte_carlo_randomness()`、`randomness_tests()`。

---

## 6. 参考号码生成策略

| 策略 | 机制 | 统计依据 |
|---|---|---|
| `hot_weighted` | 以历史频率为权重抽样前/后区号 | 高频号入样概率更高 |
| `balanced_profile` | 形态匹配：和值落在历史均值±1σ 区间、奇偶比/大小比取高频形态、区间尽量覆盖 | 和值、奇偶、大小、区间分布 |

共同约束与声明：无论何种策略，**生成的任何一注组合的中奖概率恒为 1/21,425,712**——统计权重只改变"生成器吐出什么形态的号码"，不改变任何一个具体组合的概率。生成输出逐条标注依据并强制附带不可预测性声明。

代码对照：`generate_hot_weighted()`、`generate_balanced_profile()`、`generate_reference_numbers()`。

---

## 7. 公式 ↔ 代码对照总表

| 数学对象 | 公式章节 | 代码函数 |
|---|---|---|
| 组合总数 / 奖级概率 / 自洽断言 | §1, §2 | `count_total_combinations`, `case_probability`, `case_count`, `prize_probabilities`, `self_consistency_check` |
| 蒙特卡洛概率交叉校验 | §2.3 | `monte_carlo_probability_check` |
| 期望值 / 返奖率 | §3 | `expected_value` |
| 频次 / 冷热 / 遗漏 | §4.1–4.3 | `compute_frequency`, `compute_hot_cold`, `compute_missing_values` |
| 和值理论矩 | §4.4 | `theoretical_sum_mean_variance_formula`, `theoretical_sum_distribution` |
| 奇偶 / 大小 / 区间理论分布 | §4.5–4.7 | `theoretical_odd_count_distribution`, `theoretical_big_count_distribution`, `theoretical_zone_distribution` |
| 连号 / 重号 | §4.8–4.9 | `count_consecutive_groups`, `theoretical_consecutive_group_distribution`, `theoretical_repeat_expectation` |
| 卡方 / 二项校正 / 经验 p 值 | §5 | `chi_square_uniform_test`, `per_number_binomial_test`, `monte_carlo_randomness` |
| 参考号码生成 | §6 | `generate_hot_weighted`, `generate_balanced_profile` |

---

## 8. 总声明

超级大乐透每期开奖为独立随机事件；单个 5+2 组合中奖概率恒为 1/21,425,712，与历史数据、号码选择、投注金额、投注方式完全无关。本文档全部数学模型的用途限于：描述历史样本、计算结构性概率与期望、检验历史样本与随机模型的一致性。**彩票是娱乐消费而非投资；请理性购彩，量力而行；未成年人不得购买彩票。**
